{
  "name": "simple-web",
  "version": "0.1.0",
  "description": "Sistem web sederhana: Node + Express + SQLite",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "cross-env NODE_ENV=test DB_PATH=:memory: jest --runInBand",
    "migrate": "node scripts/migrate.js",
    "seed": "node scripts/seed.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^5.1.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "jest": "^29.6.1",
    "supertest": "^6.3.3",
    "cross-env": "^7.0.3"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
